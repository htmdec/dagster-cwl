
resources:
  cwl_runner:
    config:
      executable: "cwltool"
      default_args:
        - "--enable-dev"
        - "--no-match-user"
      provenance_dir: "prov"

ops:
  run_cwl_op:
    config:
      cwl_path: "/mnt/data/dagster-cwl-src/cwl/arith_workflow.cwl"
      job_path: "/mnt/data/dagster-cwl-src/cwl/inputs.yml"
      outdir: "/mnt/data/dagster-cwl-src/outputs"
      cachedir: "/mnt/data/dagster-cwl-src/.cwl-cache"
      extra_args: []
  select_cwl_output:
    config:
      output_key: "sum_file"
